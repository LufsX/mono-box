#!/system/bin/sh

# Compatibility shim for scripts that still source box.utils directly.
scripts_dir="${scripts_dir:-$(dirname "$(realpath "$0")")}"
. "${scripts_dir}/box.network"
. "${scripts_dir}/box.clash"
